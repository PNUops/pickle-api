package kr.ac.pusan.pickle.inventory;

/** Contract schema {@code TemplateStatus}; template updates create a new version row and DISABLE the old one. */
public enum TemplateStatus {
    ACTIVE,
    DISABLED
}
