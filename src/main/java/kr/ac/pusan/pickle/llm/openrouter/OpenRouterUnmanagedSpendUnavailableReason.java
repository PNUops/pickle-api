package kr.ac.pusan.pickle.llm.openrouter;

/** Why paired unmanaged spend is deliberately absent. */
public enum OpenRouterUnmanagedSpendUnavailableReason {
    NO_BASELINE,
    INCOMPLETE_PAIR,
    RESET_BOUNDARY
}
