spring:
  application:
    name: pickle-api
  datasource:
    url: ${PICKLE_DB_URL:jdbc:postgresql://localhost:5432/pickle_dev}
    username: ${PICKLE_DB_USER:pickle}
    password: ${PICKLE_DB_PASSWORD:pickle}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

org:
  jobrunr:
    background-job-server:
      enabled: true
    dashboard:
      enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  api-docs:
    path: /api/v1/openapi

pickle:
  jwt:
    # HS256 signing key, >= 32 bytes. No default outside dev/test: startup fails
    # fast when unset (docs/plan/07 secrets policy, /etc/pickle/api.env).
    secret: ${PICKLE_JWT_SECRET:}
    access-token-ttl: 15m
  auth:
    refresh-token-ttl: 14d
    verification-token-ttl: 24h
    # Console URL the verification mail links to; token is appended as ?token=
    verification-base-url: ${PICKLE_VERIFICATION_BASE_URL:https://pickle.pnuops.com/verify-email}
  seed:
    sysadmin-email: ${PICKLE_SEED_SYSADMIN_EMAIL:admin@pickle.local}
    sysadmin-password: ${PICKLE_SEED_SYSADMIN_PASSWORD:pickle-sysadmin-dev!}
    orgadmin-email: ${PICKLE_SEED_ORGADMIN_EMAIL:orgadmin@pickle.local}
    orgadmin-password: ${PICKLE_SEED_ORGADMIN_PASSWORD:pickle-orgadmin-dev!}

---
# dev: local/LXC development. Mock mail, permissive JWT secret default.
spring:
  config:
    activate:
      on-profile: dev

pickle:
  jwt:
    secret: ${PICKLE_JWT_SECRET:pickle-dev-only-jwt-secret-0123456789abcdef}

---
# staging/prod: real SMTP (Google SMTP creds provided by operator via env).
spring:
  config:
    activate:
      on-profile: staging | prod
  mail:
    host: ${PICKLE_SMTP_HOST}
    port: ${PICKLE_SMTP_PORT:587}
    username: ${PICKLE_SMTP_USERNAME}
    password: ${PICKLE_SMTP_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

pickle:
  mail:
    from: ${PICKLE_SMTP_USERNAME}
