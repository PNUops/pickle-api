org:
  jobrunr:
    background-job-server:
      enabled: false
    job-scheduler:
      enabled: true

spring:
  flyway:
    enabled: true

pickle:
  jwt:
    secret: pickle-test-jwt-secret-0123456789abcdef-test
